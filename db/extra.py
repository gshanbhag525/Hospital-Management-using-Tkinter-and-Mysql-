# basewidth = 1080
    # baseheight = 1920
    #
    # img = Image.open("/Users/guneshs/PycharmProjects/HMSnhce/images/hospital.jpeg")
    #
    # # wpercent = (basewidth / float(img.size[0]))
    # # hsize = int((float(img.size[1]) * float(wpercent)))
    # # hsize = int(hsize * 1.65)
    # # basewidth = img.size[1]
    # baseheight = int(baseheight * 0.89)
    # basewidth = int(basewidth * 0.9258)
    # img = img.resize((baseheight, basewidth ), Image.ANTIALIAS)
    #
    # background_image = ImageTk.PhotoImage(img)
    #
    # bg_lbl = Label(root, image=background_image)
    # bg_lbl.place(x=0,y=0)
    # # bg_lbl.pack()



#LOGIN PAGE WINDOW
def Entry():
    global userbox,passbox,login,topframe,bottomframe,image_1,root
    root = tkinter.Tk()
    # root.geometry("1000x600")
    root.geometry("1920x1080")
    root.iconbitmap('/Users/guneshs/PycharmProjects/HMSnhce/images/hms.ico')


    # username=tkinter.Label(root, text="USERNAME", bg="green")
    # username.place(x=10, y=250)
    # userbox = tkinter.Entry(root)
    # userbox.place(x=10, y=280)
    #
    # password=tkinter.Label(root, text="PASSWORD")
    # passbox = tkinter.Entry(root, show="*")
    #
    # login = tkinter.Button(root, text="LOGIN", bg="green", command=GET,font="arial 8 bold")

    # heading.pack()

    # username.pack()
    # userbox.pack()
    # password.pack()
    # passbox.pack()
    # login.pack()
    # login.pack(side=tkinter.LEFT)
    # root.title("HOSPITAL MANAGEMENT SYSTEM")
    #
    #
    # root.mainloop()
    #


# Entry()

